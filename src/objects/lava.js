import {lava} from "../definitions/lava_definition.js";
import {createPlatform} from "./platform.js";

export function createLava( xPos , yPos , width ) {
    returen createPlatform(world, 0, 10, 40, lava);
}